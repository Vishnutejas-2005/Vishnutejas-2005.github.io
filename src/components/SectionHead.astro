---
interface Props {
  title: string;
  meta?: string;
}
const { title, meta } = Astro.props;
---

<div class="sec-head reveal">
  <h2 class="sec-head__title">{title}</h2>
  <span class="sec-head__rule" aria-hidden="true"></span>
  {meta && <span class="sec-head__meta label">{meta}</span>}
</div>
